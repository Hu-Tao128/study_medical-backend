spring:
  datasource:
    url: jdbc:h2:mem:studymedical;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE
    username: sa
    password: 
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      ddl-auto: create-drop
    properties:
      hibernate:
        dialect: org.hibernate.dialect.H2Dialect

  h2:
    console:
      enabled: true

  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: https://spxgotrytjkofqinsklw.supabase.co/auth/v1

app:
  dev-mode: true
  jwt:
    secret: dev-secret-key-for-testing-only-min-256-bits
    expiration: 86400000
